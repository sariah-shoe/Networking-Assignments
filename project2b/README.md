# TCP Chat Server

## How to Start
Needs to be run on Linux (WSL) or Mac for select to work properly.
Run "python client.py localhost port#"
Clients can now connect to the server with the parameters localhost and the port number